<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.tsur.wearui">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.Light">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CircularButtonActivity" />
        <activity android:name=".ActionLabelActivity" />
        <activity android:name=".ConfirmationActivityActivity" />
        <activity android:name=".DelayedConfirmationViewActivity" />
        <activity android:name=".CircularImageViewActivity" />
        <activity android:name="android.support.wearable.activity.ConfirmationActivity" />
        <activity android:name=".CardFrameActivity" />
        <activity android:name=".ProgressSpinnerActivity" />
        <activity android:name=".BoxInsetLayoutActivity" />
        <activity android:name=".WearableFrameLayoutActivity" />
        <activity android:name=".WatchViewStubActivity" />
        <activity android:name=".WearableListViewActivity" />
    </application>

</manifest>
